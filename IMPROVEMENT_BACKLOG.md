# StudyForge — Improvement Backlog

> 依据 `StudyForge_Final_Project_Report_EN_v4.docx` 的评测结论、观察到的失败模式和架构限制整理。
> 每条建议标注来源章节，并按优先级（P0 紧急 / P1 高 / P2 中 / P3 低）分级。

---

## 1. 数学公式感知的图实体提取

**优先级：P1**  
**来源：** 4B.5 What Did Not Work；3.3 图提取

**问题**  
在方程密集的幻灯片（如 `α∇L(θ)`）中，LLM 提取 prompt 将数学表达式拆成孤立符号碎片，约 30–40% 的公式类幻灯片出现图节点噪声，降低了知识图谱可读性。

**建议**  
- 在 `text_cleaner.py` 中加入 LaTeX/Unicode 数学预处理：将完整数学表达式包裹成 `[MATH_EXPR: ...]` 占位符后再送入提取 prompt，提取后再替换回原式。  
- 为提取 prompt 增加 few-shot 示例，展示如何将 `α∇L(θ)` 整体提取为单个节点 `gradient_descent_update`。  
- 对比加/不加预处理的图节点孤立率（`orphan_node_ratio`），作为量化回归指标写入 CI。

---

## 2. Exam Analysis Pipeline 的容错与降级提示

**优先级：P1**  
**来源：** 4B.5 What Did Not Work；3.8 流式事件模型

**问题**  
5 次端到端 exam analysis 运行中，2 次因 API 超时导致 trace 部分发送，UI 呈现不完整结果且错误信息未区分"网络超时 / 模型拒绝 / 索引未完成"三种原因。

**建议**  
- 在 orchestrator 中区分三类失败并写入 trace 的 `error_type` 字段：`network_timeout` / `model_refusal` / `index_incomplete`。  
- 前端根据 `error_type` 展示不同的降级消息（"提供商超时，点击重试" vs "请先完成文档索引"）。  
- 为每个 sub-agent 调用增加指数退避重试（最多 3 次），减少临时超时导致的全流程失败。

---

## 3. 检索模式选择的新用户引导

**优先级：P1**  
**来源：** 4B.5 What Surprised Us；2.3 混合检索

**问题**  
评估发现检索模式（local/global/mix）对答案质量的影响远超温度参数（0.2–0.8 范围内温度切换的差异远小于 local→global 的切换差异）。但当前 UI 把模式选择作为次要控件，新用户默认不会注意到。

**建议**  
- 首次进入 chat 时弹出 tooltip，用一句话解释三种模式的适用场景：  
  - **Local**：查定义、找公式 → 精度优先  
  - **Mix**：综合提问 → 平衡  
  - **Global**：跨章节综合 → 广度优先，慎用于精确事实查询  
- 在 chat 输入框旁常驻模式标签（而非隐藏在设置中），并记录每次查询使用的模式用于后续分析。

---

## 4. Embedding 批处理吞吐量优化

**优先级：P2**  
**来源：** 4B.5 What Surprised Us；3.2 文档摄入与分块

**问题**  
对 14 张幻灯片的 PPTX，embedding 批处理与图提取耗时相当（约 3–4 分钟）。Embedding provider 延迟而非提取 prompt 复杂度是吞吐瓶颈。

**建议**  
- 支持配置 `embedding_batch_size`（当前可能固定），允许在速率限制内最大化单批大小。  
- 引入异步并发 embedding 调用（`asyncio.gather` + semaphore 限流），与图提取流水线并行执行，而非串行。  
- 在 ingestion status API 中分别暴露 `embedding_progress` 和 `graph_extraction_progress` 两个进度字段，让用户清楚各阶段耗时。

---

## 5. Global 检索模式的自动降级护栏

**优先级：P2**  
**来源：** 4B.5 What Did Not Work；3.4 查询接口

**问题**  
Global 模式对精确事实查询的忠实度仅 50%，但目前没有任何机制提醒用户当前模式不适合当前问题类型。

**建议**  
- 在 global 模式的回答中追加轻量置信标签（如 `[synthesis mode — verify against sources]`），提示用户答案来自跨段落综合。  
- 可选：对检测到"定义类"问题（包含 "what is", "define", "formula for" 等关键词）时，在 UI 弹出建议切换到 local 模式的提示。

---

## 6. 视觉/多模态课件支持

**优先级：P2**  
**来源：** 3.10 局限与路线图

**问题**  
当前管线以文本提取为主，含图表、手绘示意图或公式截图的幻灯片在图提取中表现差，图节点缺失或语义不完整。

**建议**  
- 集成 vision-language 模型（如 `deepseek-vl` 或 `GPT-4o`）对检测到图片密度高的幻灯片页面额外提取图说（caption）。  
- 将提取的图说作为额外 chunk 送入 embedding 索引，提供与图片绑定的 `image_ref` 元数据供 UI 回溯。  
- 设置 `enable_vision_extraction` 配置开关，避免在纯文本场景中增加不必要的 API 成本。

---

## 7. 教师/助教仪表盘：图谱健康指标

**优先级：P2**  
**来源：** 3.10 路线图；6.3 部署规模

**问题**  
报告提出教师需要了解知识图谱质量（密度、孤立节点、矛盾簇），但当前 UI 仅展示索引完成状态，没有图谱健康可视化。

**建议**  
- 在 subject workspace 页面增加"Graph Health"面板，展示：  
  - 节点总数、边总数、平均度数  
  - 孤立节点比例（`orphan_node_ratio`）  
  - 与上一次索引相比的变化量  
- 后端在 `GET /api/subjects/{id}/graph/stats` 中暴露上述指标（已有 LightRAG 内部数据结构，添加聚合逻辑即可）。

---

## 8. 问题变体生成的 Bloom 分层验证

**优先级：P3**  
**来源：** 3.6 考试智能；2.6 可测试假设 C

**问题**  
Variant question 生成端点目前产出问题，但无法验证变体是否真正覆盖不同认知层级（记忆 / 理解 / 应用 / 分析）。

**建议**  
- 在生成 prompt 中加入 Bloom 层级约束（`target_bloom_level: apply`），并让模型在输出中附上自评的层级标签。  
- 构建小型评估集（每层级各 5 道人工标注题），对生成变体自动打分，作为 variant 生成质量的回归基准。

---

## 9. 文档版本与引用稳定性

**优先级：P3**  
**来源：** 2.7 协作功能；4.4 可重现性

**问题**  
当幻灯片修订后重新上传，已有对话中的引用仍指向旧文件 ID，引用链断裂或指向错误内容。

**建议**  
- 对上传文件计算内容哈希（SHA-256），在文档元数据中存储 `content_hash` 和 `version`。  
- 当检测到同名文件哈希变化时，在 UI 中提示"该文档已更新，部分引用可能指向旧版"，而非静默替换。  
- 长期：支持多版本并存，允许用户选择基于哪个版本的索引进行查询。

---

## 优先级汇总

| # | 建议 | 优先级 | 估计影响 |
|---|------|--------|----------|
| 1 | 数学公式感知图提取 | P1 | 图谱质量 +30–40% |
| 2 | Exam pipeline 容错 | P1 | 完成率 60% → 95%+ |
| 3 | 检索模式引导 | P1 | 新用户任务成功率 |
| 4 | Embedding 批处理优化 | P2 | 索引时间 ~50% |
| 5 | Global 模式降级护栏 | P2 | 精确查询误导率 ↓ |
| 6 | 视觉/多模态支持 | P2 | 图表密集课件覆盖 |
| 7 | 教师图谱健康仪表盘 | P2 | 可观测性 |
| 8 | Bloom 分层验证 | P3 | 变体生成质量 |
| 9 | 文档版本稳定性 | P3 | 引用可靠性 |
